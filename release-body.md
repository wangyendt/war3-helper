**魔兽争霸3 全能辅助** —— 经典工具 U9WSH（魔兽超级助手 v4.6, 2011）的复刻增强版。
无广告 · 无积分 · 无联网 · 不注入游戏进程 · 单文件 256KB

## 本次更新（v1.0.3）

这版修的两个 bug 是同一个根因：**助手注入的按键会继承当时按住的修饰键**。

**1. 按 Shift+物品键 会把英雄取消选中。**
开了「物品键先选中英雄」时，助手会在物品键之前注入一下 `F1`。
但你的 Shift 还物理按着，游戏收到的其实是 `Shift+F1` —— 魔兽里那是**切换**选中状态，
英雄本来选着，反而被取消了。现在注入 `F1` 之前会把按住的修饰键临时松开，发完再按回去，
所以 `Shift+物品`（排队使用）照常工作。

**2. 开了血条常显时，滚轮改键发出的键带上了 Alt。**
血条常显是靠一直按住 `Alt` 实现的，而鼠标钩子只在**点击**时松开 Alt，**漏了滚轮**。
于是滚轮改键本该发 `7`，实际发的是 `Alt+7`，编队根本没选中 ——
如果你用滚轮选商店，商店压根没被选上，接着按 `S` 当然买不到树枝。

**3. 商店模式改成常驻显示。**
以前只在切换时闪 1.8 秒，出问题时根本分不清「改键挂起了」还是「改键还在生效」。
现在挂起期间悬浮窗一直显示「商店模式 · 改键已挂起」。

### v1.0.2 修的

打字时不再被改键。按回车打开聊天栏后自动暂停改键，再按回车发送或按 Esc 取消后恢复，
状态栏显示「打字中(已暂停)」。兜底：30 秒无按键自动复位，魔兽失去前台也复位。

### v1.0.1 修的（如果你还在用 v1.0.0，务必升级）

会丢改键方案的 bug。保存配置时存在一小段主文件缺失的窗口（先删再重命名），
另一个实例正好在那一刻启动就会当作「没有配置」而套用默认值。
已改为 `File.Replace` 原子替换，加载改成 `config.json` → `config.json.bak` → 默认值三级回退。

## 下载

[`War3Helper-v1.0.3.zip`](https://github.com/wangyendt/war3-helper/releases/download/v1.0.3/War3Helper-v1.0.3.zip) —— 解压后双击 `War3Helper.exe` 即可。

只需要 Windows 自带的 .NET Framework 4.x，无需安装任何运行库。
配置保存在 `%APPDATA%\War3Helper\`，删掉程序或换目录都不会丢。

## 功能

| | |
|---|---|
| **改键** | 物品栏 6 格、任意键→任意键、鼠标侧键/中键/滚轮、多方案热切换 |
| **商店模式** | 滚一下滚轮挂起改键（商店里 `S` 还是 `S`），按 `F1` 重选英雄自动恢复 |
| **快捷喊话** | 支持 Ctrl/Alt/Shift 组合键，内置 DOTA 常用命令（默认 `Alt+1`~`Alt+0`） |
| **血条/蓝条常显** | 自动替你按住 Alt，一操作就松开，不会误发 Alt+点击信号 |
| **无边框全屏** | 按桌面分辨率启动后自动去边框铺满，Alt+Tab 秒切 |
| **局内悬浮菜单** | 左上角可拖动半透明图标，单击弹出分级菜单，不用切出游戏 |
| **版本切换** | 1.20e~1.27b 一键切换，快照式备份可完整还原 |
| **录像管理** | 自动备份每局录像，目录浏览（时长/大小/时间）、播放、重命名 |
| **其他** | 锁鼠标、老板键、屏蔽 Win 键、APM 实时显示、周期计时提醒 |

## 热键

`Ctrl+F2` 改键开关 · `Ctrl+F3` 切换方案 · `Ctrl+F4` 锁鼠标 · `Ctrl+F5` 血条常显
`Ctrl+F7` APM · `Ctrl+F8` 计时 · `Pause` 老板键

## 关于安全性

用系统级键鼠钩子实现，**不注入游戏进程、不读写游戏内存**，因此：

- 兼容 **1.20 ~ 1.28 任意版本**，切换游戏版本不用更新助手
- 没有封号风险（原版的显血显蓝走内存补丁，原版自己都提示有风险）
- 改键只在魔兽窗口为前台时生效，切出游戏完全不影响其他程序

代价是没有依赖内存注入的功能（AI 一键操作指令、JASS 脚本引擎）。

## 装完先看这个

**物品栏改键没反应？** 九成是 NumLock。魔兽的物品栏快捷键是小键盘 7/8/4/5/1/2，
NumLock 关着时这些键在系统层面就是 Home/↑/←/Clear/End/↓。程序默认会自动打开
NumLock（键盘上没有 NumLock 键也没关系，是用合成按键切换的）。

其余问题见 zip 内的《使用说明》或[项目 README](https://github.com/wangyendt/war3-helper#readme)。
